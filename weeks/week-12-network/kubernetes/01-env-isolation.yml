apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: allow-same-env-prod
  namespace: default
spec:
  podSelector:
    matchLabels:
      env: prod
  policyTypes:
    - Ingress
  ingress:
    - from:
        - podSelector:
            matchLabels:
              env: prod